<UserControl x:Class="$safeprojectname$.Views.WarehouseItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:$safeprojectname$.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:WarehouseItemsViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid AutomationProperties.AutomationId="WarehouseItemsDataGrid" ItemsSource="{Binding Path=WarehouseItems, Mode=OneWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kind" Binding="{Binding Model.Kind}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Model.Price}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Model.Quantity}" />
                <DataGridTextColumn Header="Total cost" Binding="{Binding Model.TotalCost}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
