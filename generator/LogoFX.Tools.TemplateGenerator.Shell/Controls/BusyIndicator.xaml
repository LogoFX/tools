<UserControl x:Class="LogoFX.Tools.TemplateGenerator.Shell.Controls.BusyIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300"
             Visibility="{Binding RelativeSource={RelativeSource Self}, Path=IsBusy, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Border Background="Black" Opacity="0.4" />
        <ProgressBar IsIndeterminate="True" 
                     Height="24"
                     Margin="5"
                     VerticalAlignment="Center"/>
    </Grid>
</UserControl>
