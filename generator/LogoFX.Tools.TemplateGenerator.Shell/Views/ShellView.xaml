<Window x:Class="LogoFX.Tools.TemplateGenerator.Shell.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:viewModels="clr-namespace:LogoFX.Tools.TemplateGenerator.Shell.ViewModels"
        xmlns:controls="clr-namespace:LogoFX.Tools.TemplateGenerator.Shell.Controls"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:converters="clr-namespace:LogoFX.Tools.TemplateGenerator.Shell.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:ShellViewModel}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Height="600" 
        Width="1024">
    
    <Window.Resources>
        <converters:InverseConverter x:Key="InverseConverter" />
        <converters:NotNullToBooleanConverter x:Key="NotNullToBooleanConverter" />
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <!--<CheckBox Grid.Row="0"
                  VerticalAlignment="Center"
                  Margin="5"
                  Content="Multisolution"
                  IsChecked="{Binding Path=IsMultisolution, Mode=TwoWay}" />-->
        
        <TabControl Grid.Row="1" Margin="5">
            <TabItem Header="Configuration">
                <ContentControl cal:View.Model="{Binding Path=ActiveConfiguration, Mode=OneWay}" />
            </TabItem>

            <TabItem Header="Wizard Configuration">
                <ContentControl cal:View.Model="{Binding Path=WizardConfiguration, Mode=OneWay}" />
            </TabItem>

            <TabItem Header="Root Namespaces">
                <ItemsControl Margin="5"
                              ItemsSource="{Binding Path=SolutionTemplateInfo.RootNamespaces}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="40">
                                <TextBox Text="{Binding Mode=OneWay}"
                                         IsReadOnly="True"
                                         VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2" IsEnabled="{Binding Path=CanGenerate, Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Margin="5"
                    IsEnabled="{Binding Path=IsMultisolution, Mode=OneWay, Converter={StaticResource InverseConverter}}"
                    Width="120"
                    Content="Make Multisolution">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding Path=MakeMultiSolutionCommand, Mode=OneTime}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button Grid.Column="1"
                    Margin="5"
                    IsEnabled="{Binding Path=IsMultisolution, Mode=OneWay}"
                    Width="120"
                    Content="Make Single Solution">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding Path=MakeSingleSolutionCommand, Mode=OneTime}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button Grid.Column="3"
                    Margin="5"                    
                    Width="200"
                    Content="Generate Template">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding Path=GenerateTemplateCommand, Mode=OneTime}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </Grid>

        <controls:BusyIndicator Grid.Row="0"
                                Grid.RowSpan="4"
                                IsBusy="{Binding Path=IsBusy, Mode=OneWay}"/>
    </Grid>
</Window>
